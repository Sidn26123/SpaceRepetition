const initState = {
    searchText: "",
    searchResults: [],
    learningsData: [],
    filter: [],
    sort : [],
}


const learningReducer = (state = initState, action) => {
    switch (action.type) {
        default:
            return state;
        
    }
}

export default learningReducer;