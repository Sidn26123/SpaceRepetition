/** @type {import('tailwindcss').Config} */
module.exports = {
    // darkMode: ["selector", '[data-mode="dark"]'],
    darkMode: 'selector',
    content: ["./src/**/*.{js,jsx,ts,tsx}"],
    theme: {
        extend: {},
    },
    plugins: [],
};
